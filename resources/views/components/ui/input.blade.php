@props(['label' => null, 'error' => null, 'hint' => null, 'name' => null, 'required' => false])
@php($id = $attributes->get('id', $name ?? 'input-'.uniqid()))
@php($describedBy = collect([$hint ? $id.'-hint' : null, $error ? $id.'-error' : null])->filter()->implode(' '))
<div class="ui-field"><label for="{{ $id }}" class="ui-label">{{ $label ?: $name }} @if($required)<span aria-hidden="true">*</span><span class="sr-only">(required)</span>@endif</label><input id="{{ $id }}" name="{{ $name }}" {{ $attributes->except(['id','class'])->merge(['class' => 'ui-input']) }} @required($required) @if($describedBy) aria-describedby="{{ $describedBy }}" @endif @if($error) aria-invalid="true" @endif>@if($hint)<p id="{{ $id }}-hint" class="ui-hint">{{ $hint }}</p>@endif @if($error)<p id="{{ $id }}-error" class="ui-error" role="alert" aria-live="polite">{{ $error }}</p>@endif</div>
