@props(['title' => 'Confirm action', 'message' => 'This action cannot be undone.', 'confirmLabel' => 'Confirm', 'cancelLabel' => 'Cancel', 'event' => null])
<div x-data="{ open: false }" x-cloak><button type="button" class="ui-button ui-button-danger" @click="open = true">{{ $slot ?: $confirmLabel }}</button><div x-show="open" x-trap.noscroll="open" class="ui-dialog-backdrop" role="dialog" aria-modal="true" aria-labelledby="confirm-title"><div class="ui-dialog"><h2 id="confirm-title" class="text-lg font-semibold">{{ $title }}</h2><p class="mt-2 text-sm text-slate-600">{{ $message }}</p><div class="mt-6 flex justify-end gap-2"><button type="button" class="ui-button ui-button-secondary" @click="open = false">{{ $cancelLabel }}</button><button type="button" class="ui-button ui-button-danger" @click="open = false" @if($event) wire:click="{{ $event }}" @endif wire:loading.attr="disabled">{{ $confirmLabel }}</button></div></div></div></div>
