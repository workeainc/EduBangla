<div class="space-y-6"><x-ui.breadcrumbs :items="[['label' => 'Finance', 'url' => route('student.finance', $school)], ['label' => $invoice->invoice_number]]" /><x-ui.page-header :title="$invoice->invoice_number" description="Invoice details and payment history from the server snapshot." /><x-ui.snapshot-card title="Invoice balance" :status="$invoice->status" :meta="'Due date: '.($invoice->due_date?->format('M j, Y') ?? '—')"><div class="grid gap-4 sm:grid-cols-3"><div><span class="text-sm text-slate-500">Charged</span><p class="text-lg font-semibold">{{ number_format($balance['charged_total'], 2) }} BDT</p></div><div><span class="text-sm text-slate-500">Paid</span><p class="text-lg font-semibold">{{ number_format($balance['allocated_total'], 2) }} BDT</p></div><div><span class="text-sm text-slate-500">Outstanding</span><p class="text-lg font-semibold">{{ number_format($balance['outstanding_total'], 2) }} BDT</p></div></div></x-ui.snapshot-card><x-ui.card title="Invoice items"><x-ui.data-table caption="Invoice items"><thead><tr><th>Fee</th><th>Amount</th><th>Due date</th></tr></thead><tbody>@foreach($invoice->items as $item)<tr><td data-label="Fee">{{ $item->category_name }}</td><td data-label="Amount">{{ number_format($item->amount, 2) }} BDT</td><td data-label="Due date">{{ $item->due_date?->format('M j, Y') ?? '—' }}</td></tr>@endforeach</tbody></x-ui.data-table></x-ui.card><x-ui.card title="Payment history"><x-ui.data-table caption="Payment history"><thead><tr><th>Receipt</th><th>Amount</th><th>Status</th></tr></thead><tbody>@forelse($invoice->allocations as $allocation)<tr><td data-label="Receipt">{{ $allocation->payment?->receipt_number }}</td><td data-label="Amount">{{ number_format($allocation->amount, 2) }} BDT</td><td data-label="Status"><x-ui.status-badge :status="$allocation->payment?->status" /></td></tr>@empty<tr><td colspan="3"><x-ui.empty-state title="No payments yet" /></td></tr>@endforelse</tbody></x-ui.data-table></x-ui.card></div>
