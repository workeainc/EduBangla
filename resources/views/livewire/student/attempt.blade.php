<div class="p-6 space-y-4"><h1 class="text-2xl font-bold">{{ $attempt->exam->name }} — Attempt</h1><p>Server expiry: {{ $attempt->expires_at }}</p>@if($message)<p class="text-green-700">{{ $message }}</p>@endif@foreach($attempt->questions as $q)<div class="border p-3"><p>{{ $q->sort_order }}. {{ $q->question_text }} ({{ $q->marks }})</p><input wire:model="answers.{{ $q->id }}" class="border p-2"><button wire:click="saveAnswer({{ $q->id }}, $wire.answers[{{ $q->id }}])" class="bg-blue-600 text-white p-2">Autosave</button></div>@endforeach<button wire:click="submit" class="bg-red-600 text-white p-2">Submit</button></div>
