<div class="space-y-6"><x-ui.breadcrumbs :items="[['label' => 'Finance']]" /><x-ui.page-header title="My finance" description="Invoices and balances for your student account. Amounts are calculated by the server." /><x-ui.card title="Invoices"><x-ui.data-table caption="My invoices"><thead><tr><th>Invoice</th><th>Issued / due</th><th>Charged</th><th>Outstanding</th><th>Status</th></tr></thead><tbody>@forelse($invoices as $invoice)<tr><td data-label="Invoice"><a class="font-semibold text-blue-700 underline" href="{{ route('student.finance.invoice', [$school, $invoice]) }}">{{ $invoice->invoice_number }}</a></td><td data-label="Issued / due">{{ $invoice->issued_at?->format('M j, Y') }} · {{ $invoice->due_date?->format('M j, Y') ?? '—' }}</td><td data-label="Charged">{{ number_format($invoice->charged_total, 2) }} BDT</td><td data-label="Outstanding">{{ number_format($balances[$invoice->id]['outstanding_total'], 2) }} BDT</td><td data-label="Status"><x-ui.status-badge :status="$invoice->status" /></td></tr>@empty<tr><td colspan="5"><x-ui.empty-state title="No invoices" message="Your published invoices will appear here." /></td></tr>@endforelse</tbody></x-ui.data-table></x-ui.card></div>
