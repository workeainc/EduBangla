<div class="space-y-6"><x-ui.breadcrumbs :items="[['label' => 'Exams']]" /><x-ui.page-header title="Online exams" description="Availability, timing, and attempt ownership are enforced by the server." /><x-ui.card title="Eligible exams"><x-ui.data-table caption="Eligible online exams"><thead><tr><th>Exam</th><th>Subject</th><th>Class / section</th><th>Schedule</th><th>Status</th><th></th></tr></thead><tbody>@forelse($schedules as $s)<tr><td data-label="Exam">{{ $s->exam?->name }}</td><td data-label="Subject">{{ $s->subject?->name }}</td><td data-label="Class / section">{{ $s->academicClass?->name }} · {{ $s->section?->name }}</td><td data-label="Schedule">{{ $s->scheduled_date?->format('M j, Y') }} · {{ $s->start_time }}</td><td data-label="Status"><x-ui.status-badge :status="$s->exam?->status" /></td><td data-label="Action"><a class="ui-button ui-button-primary" href="{{ route('student.exams.start',['school'=>$school,'exam'=>$s->exam]) }}">Start when available</a></td></tr>@empty<tr><td colspan="6"><x-ui.empty-state title="No eligible exams" message="Exams become visible when your active enrollment matches an available schedule." /></td></tr>@endforelse</tbody></x-ui.data-table></x-ui.card></div>
