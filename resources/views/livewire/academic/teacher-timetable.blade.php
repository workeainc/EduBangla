<div class="space-y-6">
    <x-ui.breadcrumbs :items="[['label' => 'My timetable']]" />
    <x-ui.page-header title="My timetable" description="Published timetable slots are read-only snapshots." />
    <x-ui.card title="Published schedule"><x-ui.data-table caption="My published timetable"><thead><tr><th>Day</th><th>Time</th><th>Subject</th><th>Class / section</th><th>Group</th></tr></thead><tbody>@forelse($slots as $slot)<tr><td data-label="Day">{{ ['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'][$slot->weekday] ?? $slot->weekday }}</td><td data-label="Time">{{ $slot->starts_at }}–{{ $slot->ends_at }}</td><td data-label="Subject">{{ data_get($slot->snapshot, 'subject.name', 'Subject') }}</td><td data-label="Class / section">{{ data_get($slot->snapshot, 'class.name', data_get($slot->snapshot, 'class_id', 'Class')) }} · {{ data_get($slot->snapshot, 'section.name', data_get($slot->snapshot, 'section_id', 'Section')) }}</td><td data-label="Group">{{ data_get($slot->snapshot, 'group.name', 'All groups') }}</td></tr>@empty<tr><td colspan="5"><x-ui.empty-state title="No published timetable" message="Your school has not published a timetable slot for your assignments." /></td></tr>@endforelse</tbody></x-ui.data-table></x-ui.card>
</div>
