<div class="p-6 space-y-4"><h1 class="text-2xl font-bold">Result Management</h1>@if($message)<p class="text-green-700">{{ $message }}</p>@endif<ul>@foreach($exams as $exam)<li>{{ $exam->name }} <button wire:click="compute({{ $exam->id }})" class="bg-blue-600 text-white p-1">Compute</button></li>@endforeach</ul><table class="w-full"><thead><tr><th>Student</th><th>Exam</th><th>Total</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($results as $r)<tr><td>{{ $r->student->first_name }} {{ $r->student->last_name }}</td><td>{{ $r->exam->name }}</td><td>{{ $r->total_obtained }}/{{ $r->total_marks }}</td><td>{{ $r->status }}</td><td>@if($r->status==='computed')<button wire:click="lock({{ $r->id }})">Lock</button>@elseif($r->status==='locked')<button wire:click="publish({{ $r->id }})">Publish</button>@endif</td></tr>@empty<tr><td colspan="5">কোনো result নেই।</td></tr>@endforelse</tbody></table></div>
