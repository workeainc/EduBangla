<div class="p-6 space-y-4"><h1 class="text-2xl font-bold">Report Cards</h1><h2>Eligible results</h2>@foreach($results as $r)<button wire:click="generate({{$r->id}})" class="mr-2">Generate #{{$r->id}}</button>@endforeach<table class="w-full"><tbody>@foreach($cards as $c)<tr><td>{{$c->exam->name}}</td><td>{{$c->student->first_name}} {{$c->student->last_name}}</td><td>{{$c->status}}</td><td>@if($c->status==='generated')<button wire:click="publish({{$c->id}})">Publish</button>@endif</td></tr>@endforeach</tbody></table></div>
