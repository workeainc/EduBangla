<div class="p-6 space-y-4"><h1 class="text-2xl font-bold">Question Version {{ $version->version }}</h1><p><strong>প্রশ্ন:</strong> {{ $version->question->stable_key }} ({{ $version->question->type }})</p><p>{{ $version->prompt }}</p><p>Marks: {{ $version->marks }} · তৈরি: {{ $version->created_at }}</p>@if($version->options->isNotEmpty())<h2 class="font-semibold">Options</h2><ul>@foreach($version->options as $option)<li>{{ $option->option_key }}. {{ $option->option_text }} @if($option->is_correct)<span class="text-green-700">(সঠিক উত্তর)</span>@endif</li>@endforeach</ul>@endif<p class="text-gray-600">এই version historical হলে read-only।</p></div>
