<div class="p-6 space-y-4"><h1 class="text-2xl font-bold">{{ $question->stable_key }} — Versions</h1>@if($message)<p class="text-green-700">{{ $message }}</p>@endif<form wire:submit="save" class="space-x-2"><textarea wire:model="form.prompt" placeholder="Question content" class="border p-2"></textarea><input wire:model="form.marks" placeholder="Marks" class="border p-2"><button class="bg-blue-600 text-white p-2">New version</button></form><form wire:submit="saveOption" class="space-x-2"><input wire:model="optionForm.option_key" placeholder="Option key" class="border p-2"><input wire:model="optionForm.option_text" placeholder="Option text" class="border p-2"><button class="bg-blue-600 text-white p-2">Option যোগ</button></form><ol>@forelse($versions as $v)<li class="border p-2">v{{ $v->version }} — {{ $v->prompt }} — {{ $v->marks }} marks @if($v->options->isNotEmpty())<ul>@foreach($v->options as $o)<li>{{ $o->option_key }}: {{ $o->option_text }} @if($o->is_correct)✓@endif <button wire:click="markCorrect({{ $o->id }})">সঠিক</button> <button wire:click="deleteOption({{ $o->id }})">মুছুন</button></li>@endforeach</ul>@endif</li>@empty<li>No versions.</li>@endforelse</ol></div>
