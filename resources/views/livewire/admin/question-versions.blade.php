<div class="p-6 space-y-4"><h1 class="text-2xl font-bold">{{ $question->stable_key }} — Versions</h1>@if($message)<p class="text-green-700">{{ $message }}</p>@endif<form wire:submit="save" class="space-x-2"><textarea wire:model="form.prompt" placeholder="Question content" class="border p-2"></textarea><input wire:model="form.marks" placeholder="Marks" class="border p-2"><button class="bg-blue-600 text-white p-2">New version</button></form><ol>@forelse($versions as $v)<li>v{{ $v->version }} — {{ $v->prompt }} — {{ $v->marks }} marks</li>@empty<li>No versions.</li>@endforelse</ol></div>
