<div class="space-y-6"><x-ui.breadcrumbs :items="[['label' => 'Academic setup', 'url' => route('admin.academic-setup', $school)], ['label' => 'Academic groups']]" /><x-ui.page-header title="Academic groups" description="Groups are optional and are only needed when your school uses streams or group-specific subjects." />
<x-ui.card title="Create academic group"><form wire:submit="save" class="grid gap-4 sm:grid-cols-2"><x-ui.input label="Group name" wire:model="name" :error="$errors->first('name')" required /><x-ui.input label="Group code" wire:model="code" :error="$errors->first('code')" required /><div class="sm:col-span-2">@if($errors->any())<x-ui.alert type="error">{{ $errors->first() }}</x-ui.alert>@endif <x-ui.button type="submit" loading="save">Create group</x-ui.button></div></form></x-ui.card>
<x-ui.card title="Configured academic groups"><x-ui.data-table caption="Academic groups"><thead><tr><th>Group</th><th>Code</th><th>Status</th></tr></thead><tbody>@forelse($groups as $group)<tr><td data-label="Group">{{ $group->name }}</td><td data-label="Code">{{ $group->code }}</td><td data-label="Status"><x-ui.status-badge :status="$group->status" /></td></tr>@empty<tr><td colspan="3"><x-ui.empty-state title="No academic groups" message="Groups are optional. Create one only if your school uses group-specific academic setup." /></td></tr>@endforelse</tbody></x-ui.data-table></x-ui.card></div>
