<div class="p-6 space-y-4"><h1 class="text-2xl font-bold">Promotions</h1><table class="w-full"><tbody>@forelse($promotions as $p)<tr><td>{{ $p->student->first_name }} {{ $p->student->last_name }}</td><td>{{ $p->targetClass->name }} / {{ $p->targetAcademicYear->name }}</td><td>{{ $p->status }} ({{ $p->decision }})</td><td>@if($p->status==='draft')<button wire:click="evaluate({{$p->id}})">Evaluate</button>@elseif($p->status==='eligible')<button wire:click="approve({{$p->id}})">Approve</button>@elseif($p->status==='approved')<button wire:click="apply({{$p->id}})">Apply</button>@endif @if(!in_array($p->status,['applied','cancelled']))<button wire:click="cancel({{$p->id}})">Cancel</button>@endif</td></tr>@empty<tr><td>No promotions.</td></tr>@endforelse</tbody></table></div>
