<div class="p-6 space-y-4"><h1 class="text-2xl font-bold">{{ $exam->name }} — Correction History</h1><table class="w-full"><thead><tr><th>Student</th><th>Subject</th><th>Actor</th><th>Previous</th><th>New</th><th>Reason</th><th>Date</th></tr></thead><tbody>@forelse($logs as $log)<tr><td>{{ $log->after['student_id'] ?? '-' }}</td><td>{{ $log->after['subject_id'] ?? '-' }}</td><td>{{ $log->actor->name ?? 'Admin #'.$log->actor_id }}</td><td>{{ $log->before['marks'] ?? '-' }}</td><td>{{ $log->after['marks'] ?? '-' }}</td><td>{{ $log->after['reason'] ?? '-' }}</td><td>{{ $log->created_at }}</td></tr>@empty<tr><td colspan="7">কোনো correction history নেই।</td></tr>@endforelse</tbody></table></div>
