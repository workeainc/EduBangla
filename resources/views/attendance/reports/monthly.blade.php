<x-layouts.app><div class="space-y-6"><x-ui.breadcrumbs :items="[['label' => 'Attendance'], ['label' => 'Monthly report']]" /><x-ui.page-header title="Monthly attendance report" description="Attendance totals and percentages returned by the reporting contract." />@include('attendance.reports._filters', ['report' => 'monthly'])<x-ui.card title="Monthly summary"><x-ui.data-table caption="Monthly attendance"><thead><tr><th>Student</th><th>Total</th><th>Present</th><th>Absent</th><th>Late</th><th>Excused</th><th>Attendance</th></tr></thead><tbody>@forelse($rows as $row)<tr><td data-label="Student">{{ trim($row['s']->first_name.' '.$row['s']->last_name) }}</td><td data-label="Total">{{ $row['d'] }}</td><td data-label="Present">{{ $row['a']->present ?? 0 }}</td><td data-label="Absent">{{ $row['a']->absent ?? 0 }}</td><td data-label="Late">{{ $row['a']->late ?? 0 }}</td><td data-label="Excused">{{ $row['a']->excused ?? 0 }}</td><td data-label="Attendance"><x-ui.status-badge :status="($row['percentage'] >= 75 ? 'present' : 'absent')" /> {{ $row['percentage'] }}%</td></tr>@empty<tr><td colspan="7"><x-ui.empty-state title="No students found" message="No attendance data exists for this month." /></td></tr>@endforelse</tbody></x-ui.data-table></x-ui.card></div></x-layouts.app>
