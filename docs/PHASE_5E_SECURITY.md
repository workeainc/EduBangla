# Phase 5E Security
Actions re-resolve school, student, enrollment, class, academic year and published ReportCard server-side. Admin-only mutations are tenant scoped; teacher and student screens are read-only. Applied/cancelled records cannot be mutated and target enrollment creation is transactional.
