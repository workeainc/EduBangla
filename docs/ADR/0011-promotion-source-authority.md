# ADR 0011: Promotion Source Authority
Promotion evaluates only a published ReportCard for the active source enrollment and an active tenant PromotionRule. Application creates a new target enrollment transactionally and preserves the source history. Historical academic evidence is never rewritten.
